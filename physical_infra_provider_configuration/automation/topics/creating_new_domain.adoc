==== Creating a new Domain

. Log in to CloudForms Management Engine as a user who has permission to create a new domain. The default user is admin, password smartvm.

. Navigate to Automation → Automate → Explorer and select *(Explorer)*.

. Cick **(Configuration)**, then click **(Add a New Domain)**.

. Enter a name for the domain and check as enable.

. Click **Add**.